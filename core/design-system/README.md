# Dependency Diagram

```mermaid
%%{
  init: {
    'theme': 'dark'
  }
}%%

graph TB
  subgraph core
    design-system
  end

```
# :core:design-system

This module contains custom compose based design system with custom components for the app to use.
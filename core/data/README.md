# Dependency Diagram

```mermaid
%%{
  init: {
    'theme': 'dark'
  }
}%%

graph TB
  subgraph core
    data
  end

```
# :core:data

This module contains domain data types definitions for whole project